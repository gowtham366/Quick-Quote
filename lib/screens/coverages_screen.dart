import 'package:flutter_web/material.dart';

class PolicyCoveragesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Coverages',
          style: TextStyle(fontSize: 20),
        ),
        centerTitle: true,
      ),
    );
  }
}
